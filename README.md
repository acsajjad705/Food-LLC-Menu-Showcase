# Food, LLC — Responsive Menu Demo

This project is the **Module 3 Solution** for the Coursera course *HTML, CSS, and Javascript for Web Developers*.  
It demonstrates a responsive, single‑page restaurant menu built with **Bootstrap 3.3**.

---

## ✨ Features

- **Single page** implementation (`index.html`).
- **Bootstrap 3.3** used for layout and responsiveness.
- **No inline styles** — all CSS in `css/style.css`.
- **Navbar scrolls away** with the page (not fixed).
- **Brand** aligned left as *Food, LLC*.
- **Mobile‑only full‑width dropdown** menu with items: Chicken, Beef, Sushi.
- **Centered page heading** using Bootstrap `text-center`.
- **Tall content section** (`min-height` set) to force scrolling.

---

## ▶️ How to View Locally

1. Clone or download the repository.
2. Open `module3-solution/index.html` in your browser.

---

## 🌐 How to Publish on GitHub Pages

1. Create a repository on GitHub (e.g., `repo-name`).
2. Add the `module3-solution` folder and files.
3. Push to GitHub:

```bash
git init
git add module3-solution
git commit -m "Add Module 3 solution"
git remote add origin https://github.com/YourGitHubId/repo-name.git
git push -u origin main


